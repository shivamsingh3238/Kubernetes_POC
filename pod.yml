apiVersion: v1
kind : Pod
metadata:
  name: my-pod
  labels:
    app: mypod
    type: my-first-app

spec:
  containers:
    - name: nginx-container
      image: nginx
      ports:
        - containerPort: 80
